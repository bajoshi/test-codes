from __future__ import division

import numpy as np
import os
import sys

import matplotlib.pyplot as plt

if __name__ == '__main__':

    # get the equation for the arc of a great circle
    # see the references from Wolfram
    
    # plot
    fig = plt.figure(figsize=(8,4))
    ax = fig.add_subplot(111)

    # convert spherical to cartersian
    for i in range(len(lon_arr)):

        x = 
        y = 

        ax.plot(x, y, 'o', color='k', markersize=2, markeredgecolor='k')

    plt.show()

    sys.exit(0)